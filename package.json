{
  "name": "obsidian-media-transcript",
  "version": "1.0.10",
  "description": "Obsidian plugin: media player with synchronized transcript",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit --skipLibCheck && vitest run && node esbuild.config.mjs production",
    "typecheck": "tsc --noEmit --skipLibCheck",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [],
  "author": "semsevens",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "0.17.3",
    "obsidian": "latest",
    "tslib": "2.4.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
